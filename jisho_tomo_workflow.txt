1. Export TSV from Jisho Tomo
2. Import into Anki, select Vocab Megadeck, changing 3rd field to Reading instead of Heisig keywords, select Allow HTML. Trying out "Update existing note when first field matches" so that if I already have the word, it gets updated and tagged instead of silently ignored.
3. Tag new cards with "add_keywords"
4. Run script oneoff_heisigkeywords_fromcards.py
5. Check output in cards
6. Reschedule for tomorrow
7. Clear favorites in Jisho Tomo
